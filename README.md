State the main problem.
Set up the Physics Informed Neural Network (PINN).
Define the loss function as the sum of the PDE residual loss, the BC loss and the IC loss.
Generate training data and train the PINN for 10000 epochs.
Compare the analytical solution and the approximate solution.
